<wxs module="main" src="./index.wxs" />

<view
  id="move-wrapper"
  class="{{ mpClass }}"
  bind:touchstart="{{ main.onTouchStart }}"
  bind:touchmove="{{ main.onTouchMove }}"
  bind:touchend="{{ main.onTouchEnd }}"
  data-stop="{{ stopClose }}"
>
  <slot />
</view>
